# Functional QA Notes

The deterministic `?demo` path started a mixed five-question round, advanced through addition, subtraction, multiplication, division, and sequence questions, and reached the summary screen with 100% accuracy, 00:06 active time, 1.0s average pace, 0 hints, and five per-question detail rows. The result was also written to the recent journal via localStorage.

The browser console reported no output or runtime errors during the demo. The summary remains readable in the browser at a phone-sized viewport and offers both “Back to paths” and “Play this path again” actions.
